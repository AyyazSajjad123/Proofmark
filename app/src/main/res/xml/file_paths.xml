<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- App-internal files (Context.getFilesDir()) -->
    <files-path
        name="files"
        path="." />

    <!-- App-internal cache (Context.getCacheDir()) -->
    <cache-path
        name="cache"
        path="." />

    <!-- OPTIONAL: future reports folder under files/ -->
    <!-- <files-path name="reports" path="reports/" /> -->

    <!-- OPTIONAL: if you plan to use getExternalFilesDir("ProofMark/") -->
    <!-- <external-files-path name="external_files" path="ProofMark/"/> -->

</paths>
